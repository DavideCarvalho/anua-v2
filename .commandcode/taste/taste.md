# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# git
- Push direct to master, never create PRs. Use `git push origin <branch>:master` or just push to main. Confidence: 0.95
- No Co-Authored-By trailer in commits. Confidence: 0.95

# typescript
- Avoid loose TypeScript types: never use `as`, `unknown`, `any`, `never`. Use type guards and proper generics instead. Confidence: 0.95
- Use `await` instead of `void` for async operations. Confidence: 0.80

# dependencies
- Pin dependency versions exactly. Never use `>=`, `^`, or `~` for new package entries. Confidence: 0.90

# adonis
- Use barrel files (`#generated/controllers`) for all route controller imports instead of individual imports. Confidence: 0.85
- Never edit `.adonisjs/server/controllers.ts` directly — it is auto-generated by the dev server. Confidence: 0.85

# testing
- Use chrome-devtools MCP for browser testing on localhost:3333. Confidence: 0.85
- Only test on "escola teste completo 3" — never on production schools like "Silva Gomes" unless explicitly asked. Confidence: 0.85

# ai-chat
- Never expose raw camelCase tool names in AI chat UI. Use PT-BR labels or generic fallbacks like "Trabalhando…". Confidence: 0.80
- Use `staleTime: 0` and `refetchOnMount: 'always'` for queries that depend on data that may have been modified by backend actions. Confidence: 0.70

# anua-frontend
- Use Tuyau inline for TanStack Query calls: `api.api.v1.X.queryOptions(...)` directly. Never create wrapper query layers. Confidence: 0.90
- Always use user-facing PT-BR text in UI. No English labels. Confidence: 0.85
- Use `ring-1 ring-foreground/10` instead of `shadow-sm` for card elevation to follow flat-by-default design. Confidence: 0.75

# adonis-transformers
- Use AdonisJS v7 transformers (`BaseTransformer` from `@adonisjs/core/transformers`) instead of old DTO patterns. Always call `serialize()` from HttpContext when returning transformer output. Confidence: 0.85
- Include all model fields and relationships in transformers. If a model has a relationship, the transformer must expose it. Confidence: 0.85
- Use `Transformer.paginate(data, metadata)` for paginated responses instead of custom serializers or manual pagination logic. Confidence: 0.80
- Use `this.whenLoaded()` or `.depth()` to control relationship serialization depth — never manually avoid including relationships to prevent recursion. Confidence: 0.75

# infrastructure
- Configure schedules/cron jobs through Terraform, not manually or in-app. Confidence: 0.70

# backend
- Backend controllers should obtain context data (like school IDs) from middleware/auth context, not require it as client-sent parameters. Confidence: 0.65
